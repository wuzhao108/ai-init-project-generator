<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>{{ config.package }}</groupId>
        <artifactId>{{ config.name }}</artifactId>
        <version>{{ config.version }}</version>
    </parent>

    <artifactId>{{ config.name }}-{{ module_name }}</artifactId>
    <packaging>{% if is_web_module %}jar{% else %}jar{% endif %}</packaging>
    <name>{{ config.name }}-{{ module_name }}</name>
    <description>{{ config.description }} - {{ module_name }} module</description>

    <dependencies>
        {% if module_name == 'common' %}
        <!-- Spring Boot Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        
        {% elif module_name == 'api' %}
        <!-- Common Module -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>{{ config.name }}-common</artifactId>
        </dependency>
        
        <!-- Swagger -->
        {% if config.tech_stack.doc %}
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger2</artifactId>
        </dependency>
        {% endif %}
        
        {% elif module_name == 'dao' %}
        <!-- Common Module -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>{{ config.name }}-common</artifactId>
        </dependency>
        
        {% if config.tech_stack.orm == 'mybatis' %}
        <!-- MyBatis -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
        </dependency>
        {% elif config.tech_stack.orm == 'jpa' %}
        <!-- JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        {% endif %}
        
        <!-- Database Driver -->
        {% if config.tech_stack.database == 'mysql' %}
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <scope>runtime</scope>
        </dependency>
        {% elif config.tech_stack.database == 'postgresql' %}
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        {% elif config.tech_stack.database == 'h2' %}
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        {% endif %}
        
        {% elif module_name == 'service' %}
        <!-- API Module -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>{{ config.name }}-api</artifactId>
        </dependency>
        
        <!-- DAO Module -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>{{ config.name }}-dao</artifactId>
        </dependency>
        
        <!-- Spring Boot Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        
        {% if 'redis' in config.tech_stack.cache %}
        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        {% endif %}
        
        {% if 'rabbitmq' in config.tech_stack.mq %}
        <!-- RabbitMQ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        {% endif %}
        
        {% if 'kafka' in config.tech_stack.mq %}
        <!-- Kafka -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
        {% endif %}
        
        {% elif module_name == 'web' %}
        <!-- Service Module -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>{{ config.name }}-service</artifactId>
        </dependency>
        
        <!-- Spring Boot Web Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        
        {% if config.tech_stack.actuator %}
        <!-- Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        {% endif %}
        
        {% if config.tech_stack.doc %}
        <!-- Swagger UI -->
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger-ui</artifactId>
        </dependency>
        {% endif %}
        
        {% if config.tech_stack.security %}
        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        {% endif %}
        
        {% endif %}
        
        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    {% if is_web_module %}
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
    {% endif %}
</project>